# コメントを追加したらサーバー再起動
type Query {
  user(id: String!): User
}

type Mutation {
  """
  引数(ユーザー名、エンジニアタイプ、GithubURL?)
  """
  createUser(user: UserCreateInput!): createdUser!
  updateUser(id: String!, user: UserUpdateInput!): User!
}
